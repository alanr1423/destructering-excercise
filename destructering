8 and 1846

{ yearNeptuneDiscovered: 1846, yearMarsDiscovered: 1659 }

“Your name is Alejandro and you like purple”.
“Your name is Melissa and you like green.”
“Your name is undefined and you like green”

let [first, second, third] = ["Maya", "Marisa", "Chi"]; 
console.log(first); Maya
console.log(second); Marisa 
console.log(third); Chi

“Raindrops on roses” for the first log, for the second, “whiskers on kittens” would be logged and the third would be ["Bright copper kettles", "warm woolen mittens", "Brown paper packages tied up with strings"]

[10, 30, 20]

const obj = {
  numbers: {
    a: 1,
    b: 2
  }
};
Const {a,b} = obj.numbers

.let arr = [1, 2];
[arr[0], arr[1]] = [arr[1], arr[0]];


raceResults(['Tom', 'Margaret', 'Allison', 'David', 'Pierre'])

// {first: "Tom", second: "Margaret", third: "Allison", rest: ["David", "Pierre"]}

const raceResults = ([first, second, third, ...rest]) => ({first, second, third, rest})

des
